# Legacy dashboard (quarantined)

The `app/(dashboard)/` route group was an early AI-ingest prototype. It is **not** part of the Fractals Platform commercial path.

- Not linked from platform login routing
- Not protected by default middleware prefixes
- Kept in-repo for reference only; do not extend for FF V1

Platform routes: `/admin`, `/distributor`, `/client/*`, `/{domain}/*`
